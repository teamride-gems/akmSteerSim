policy: "MlpPolicy"
learning_rate: 0.0003
batch_size: 256
tau: 0.005
gamma: 0.99
seed: 42
train_steps: 500000
save_every_steps: 25000
eval_interval_steps: 10000
ent_coef: "auto"

policy_kwargs:
  net_arch: [256, 256]

max_episode_steps: 3000

heldout_tracks: [Austin, BrandsHatch, Budapest, Hockenheim]