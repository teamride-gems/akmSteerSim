# Frank opaque operator inputs

Use only `operator_schedule.csv` and the ROS 1 runner. Conditions are represented only by opaque codes in this directory. Do not inspect or compare packet bundles during collection. Semantic unblinding is held by the study lead until all outcomes are locked.
